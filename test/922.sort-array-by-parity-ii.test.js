const sortArrayByParityII = require('../src/922.sort-array-by-parity-ii.js');

test('Example 1', () => {
  expect(sortArrayByParityII([4, 2, 5, 7])).toEqual([4, 5, 2, 7]);
});

test('Example 1', () => {
  expect(sortArrayByParityII([2, 3])).toEqual([2, 3]);
});