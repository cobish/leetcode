const findComplement = require('../src/476.number-complement.js');

test('Example 1', () => {
  expect(findComplement(5)).toBe(2);
});

test('Example 2', () => {
  expect(findComplement(1)).toBe(0);
});